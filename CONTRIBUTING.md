# Contributing

این مخزن فعلاً با یک maintainer اصلی توسعه داده می‌شود. هدف، تغییرات کوچک، قابل تست و قابل بازبینی است.

## روند کار

1. از `main` یک branch کوتاه‌عمر بساز.
2. فقط یک مسئله را در هر branch حل کن.
3. از Conventional Commits استفاده کن.
4. پیش از Pull Request اجرا کن:

```bash
python scripts/check_all.py
```

5. تست دستی همان نسخه را از `USER_VERIFICATION.md` انجام بده.
6. فقط وقتی CI سبز است merge کن.

## نام branch

- `feat/...` قابلیت جدید
- `fix/...` رفع باگ
- `test/...` پوشش تست
- `docs/...` مستندات
- `chore/...` زیرساخت و نگهداری

## قانون معماری

- abstraction جدید فقط وقتی اضافه می‌شود که دست‌کم دو مصرف واقعی داشته باشد.
- تغییر UI نباید منطق دامنه را داخل JavaScript پنهان کند.
- تغییر schema باید همراه تست validation باشد.
- داده کاربر و فایل‌های `data/` نباید commit شوند.
