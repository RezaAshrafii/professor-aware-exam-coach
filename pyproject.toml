[project]
name = "professor-aware-exam-coach"
version = "0.3.0"
description = "Local-first professor-aware university exam preparation coach"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
