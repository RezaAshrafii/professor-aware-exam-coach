# Git history for v0.7.0

Branch:

```text
feat/model-runtime
```

Planned commits:

```text
feat(models): add generic provider connections and dynamic discovery
feat(web): add compact searchable model settings
build(startup): skip unchanged dependency installation
 test(models): cover provider runtime and secret masking
 docs(release): document v0.7.0 verification and GitHub update
```

After verification the branch is merged into `main` and tagged `v0.7.0`.
