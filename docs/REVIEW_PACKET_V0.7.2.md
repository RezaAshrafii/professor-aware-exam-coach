# External Review Packet — v0.7.2

Review the diff from v0.7.1 to v0.7.2 with emphasis on:

1. whether separate question/answer/score input materially reduces friction;
2. whether grading output remains readable in Persian RTL with embedded formulas;
3. whether Persian-language validation is strict enough without rejecting mathematical notation;
4. whether prior student mistakes are clearly separated from professor evidence;
5. whether the two-file Windows updater preserves Git, data, secrets and environments safely;
6. whether any remaining feature can be replicated trivially by a generic LLM prompt.

Reject the release for data loss, secret exposure, fabricated professor-specific claims, inconsistent score totals or repeated dependency installation on unchanged requirements.
