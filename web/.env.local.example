# Browser requests use the same-origin /backend proxy by default.
# Set NEXT_PUBLIC_API_URL only when deliberately connecting to a remote API.
NEXT_PUBLIC_API_URL=
BACKEND_INTERNAL_URL=http://127.0.0.1:8000
