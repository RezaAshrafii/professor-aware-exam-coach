# User Verification — v0.4.0

برای رفتن به نسخه بعد فقط سه تست دستی لازم است. بقیه مسیرها با تست خودکار پوشش داده شده‌اند.

## ۱. تست پایه — الزامی

1. `start_windows.bat` را اجرا کن.
2. یک درس بساز.
3. یک فایل TXT بارگذاری کن.
4. حالت «آموزش مبحث» را اجرا کن.

نتیجه درست:

- برنامه بالا بیاید؛
- فایل دیده شود؛
- پاسخ و evidence نمایش داده شوند.

## ۲. تست Draft Example Card — الزامی

1. در تب «مثال‌های استاد» یک کارت بساز.
2. وضعیت را `پیش‌نویس` بگذار.
3. همان عنوان یا روش را در حالت آموزش جست‌وجو کن.

نتیجه درست:

- کارت در UI دیده شود؛
- برچسب پیش‌نویس داشته باشد؛
- در evidence پاسخ مدل ظاهر نشود.

معیار رد: کارت پیش‌نویس برای مدل ارسال شود.

## ۳. تست Confirmed Example Card — الزامی

1. کارت مرحله قبل را با منبع اصلی تطبیق بده.
2. روی «تأیید کارت» بزن.
3. دوباره درباره همان مثال سؤال کن.

نتیجه درست:

- evidence با عنوان `کارت مثال: ...` دیده شود؛
- نوع شاهد «کارت مثال تأییدشده» باشد؛
- پاسخ تا حد ممکن از روش ثبت‌شده همان کارت استفاده کند؛
- کارت را بتوانی دوباره به پیش‌نویس برگردانی.

معیار رد:

- کارت تأییدشده بازیابی نشود؛
- کارت متعلق به درس دیگری وارد پاسخ شود؛
- کارت پیش‌نویس و تأییدشده رفتار یکسان داشته باشند.

## تست دستی لازم نیست

این موارد با تست خودکار پوشش داده شده‌اند:

- ساخت جدول `example_cards`؛
- create/list/status persistence؛
- جلوگیری از تغییر کارت با course_id درس دیگر؛
- حذف کارت‌های draft از retrieval؛
- ورود کارت confirmed به retrieval؛
- workflowهای قبلی course/upload/run/mistake؛
- structured-output validation و citation validation.

## Gate نسخه بعد

برای شروع `v0.5.0` هر سه تست بالا باید روی حداقل یک درس واقعی پاس شوند.
