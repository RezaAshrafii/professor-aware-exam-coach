# User Verification — v0.6.0

## ۱. اتصال برنامه — الزامی

1. تمام پنجره‌های نسخه قبلی را ببند.
2. `start_product_windows.bat` را اجرا کن.
3. برنامه باید خودش بعد از آماده‌شدن در `http://localhost:3000` باز شود.

نتیجه درست:

- بالای داشبورد «هسته متصل است» دیده شود؛
- در DevTools → Network، درخواست `health` باید status `200` داشته باشد؛
- نباید `CORS error` یا preflight `405` ببینی.

## ۲. تست مسیر اصلی — الزامی

- یک درس بساز؛
- یک فایل TXT کوچک بارگذاری کن؛
- یک Example Card پیش‌نویس بساز و تأییدش کن؛
- صفحه را refresh کن.

همه داده‌ها باید باقی بمانند و هیچ درخواست قرمزی در Network نباشد.

## ۳. ظاهر و responsive — بازخورد لازم

این سه صفحه را بررسی کن:

- داشبورد؛
- نمای کلی یک درس؛
- مربی پاسخ و نتیجه structured grading.

عرض مرورگر را هم تا حالت موبایل کوچک کن. فقط مشکلات واقعی خوانایی، شلوغی، فاصله یا بیرون‌زدگی را گزارش کن.

## تست دستی لازم نیست

این موارد خودکار تست شده‌اند:

- preflight دقیق `/health` با origin `127.0.0.1:3000` و header `content-type`؛
- proxy پیش‌فرض `/backend`؛
- عدم ارسال `Content-Type` روی GET بدون body؛
- CRUD درس، منبع، مثال و خطا؛
- structured output و citation validation؛
- syntax فایل‌های TypeScript.

## Gate نسخه بعد

اگر اتصال سبز است، CRUD از UI کار می‌کند و خطای CORS دیده نمی‌شود، این نسخه از نظر ارتباط frontend/backend قابل اتکاست.
