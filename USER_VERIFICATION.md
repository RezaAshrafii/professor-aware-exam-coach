# User Verification — v0.5.0

این نسخه برای اولین بار UI واقعی Next.js دارد. قبل از توسعه قابلیت بعدی، این تست‌ها را انجام بده.

## ۱. بالا آمدن محصول — الزامی

1. `start_product_windows.bat` را اجرا کن.
2. صبر کن صفحه `http://localhost:3000` باز شود.
3. بالای صفحه باید «هسته متصل است» دیده شود.

رد نسخه:

- صفحه سفید یا خطای build؛
- backend وصل نباشد؛
- صفحه فقط HTML قدیمی روی پورت ۸۰۰۰ باشد.

## ۲. ساخت درس — الزامی

1. روی «ساخت درس جدید» بزن.
2. نام درس، استاد، ترم و نمره هدف را وارد کن.
3. فرم را ذخیره کن.

نتیجه درست:

- وارد workspace همان درس شوی؛
- اطلاعات درس در sidebar و نمای کلی درست باشد؛
- با برگشت به داشبورد، کارت درس دیده شود.

## ۳. منبع و Example Card — الزامی

1. از تب «منابع» یک TXT کوچک بارگذاری کن.
2. از تب «مثال‌های استاد» یک کارت پیش‌نویس بساز.
3. آن را تأیید کن و دوباره به پیش‌نویس برگردان.

نتیجه درست:

- منبع در فهرست دیده شود؛
- وضعیت کارت فوراً تغییر کند؛
- refresh اطلاعات را از بین نبرد.

## ۴. مربی پاسخ — الزامی

1. تب «مربی پاسخ» را باز کن.
2. در حالت Demo یک درخواست متنی اجرا کن.
3. سپس حالت «تصحیح سخت‌گیرانه» را اجرا کن.

نتیجه درست:

- خروجی داخل همان صفحه نمایش داده شود؛
- evidence دیده شود؛
- GradingReport به‌شکل جدول و بخش‌های جدا نمایش داده شود، نه JSON خام؛
- اجرا در تاریخچه ذخیره شود.

## ۵. واکنش‌گرایی — الزامی

عرض مرورگر را کوچک کن یا با گوشی در شبکه محلی باز کن.

نتیجه درست:

- sidebar با دکمه منو باز و بسته شود؛
- فرم‌ها و جدول‌ها از صفحه بیرون نزنند؛
- عملیات اصلی قابل استفاده بمانند.

## تست دستی لازم نیست

با تست خودکار پوشش داده شده‌اند:

- CRUD JSON برای درس؛
- upload و حذف منبع؛
- create/status/delete مثال؛
- جداسازی داده دو درس؛
- create/delete خطا؛
- CORS برای `localhost:3000`؛
- تمام تست‌های structured output و citation validation؛
- syntax ده فایل TypeScript.

## Gate نسخه بعد

هر پنج تست بالا باید پاس شوند. مهم‌ترین blocker، شکست `npm run build` یا مشکل در CRUD از UI است.
